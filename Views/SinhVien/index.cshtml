@model IEnumerable<CSDLNC_QuanLySVNamTot.Models.SinhVien>

@{
   ViewData["Title"] = "Danh sách sinh viên";
}

<div class="container mt-4">
   <h2 class="text-center text-primary fw-bold">📌 Danh Sách Sinh Viên</h2>

   <div class="d-flex justify-content-between align-items-center mb-3">
      <a href="@Url.Action("Create")" class="btn btn-success">
         <i class="fas fa-user-plus"></i> Thêm Sinh Viên
      </a>
   </div>

   <div class="table-responsive">
      <table class="table table-bordered table-hover shadow-sm">
         <thead class="table-dark text-center">
            <tr>
               <th>Mã SV</th>
               <th>Họ Tên</th>
               <th>Ngày Sinh</th>
               <th>Email</th>
               <th>Số ĐT</th>
               <th>Lớp</th>
               <th>Hành Động</th>
            </tr>
         </thead>
         <tbody class="text-center align-middle">
            @foreach (var sv in Model)
            {
               <tr>
                  <td>@sv.MaSV</td>
                  <td>@sv.HoTen</td>
                  <td>@sv.NgaySinh.ToString("dd/MM/yyyy")</td>
                  <td>@sv.Email</td>
                  <td>@sv.SoDienThoai</td>
                  <td>@sv.Lop.TenLop</td>
                  <td>
                     <a href="@Url.Action("Edit", new { id = sv.MaSV })" class="btn btn-warning btn-sm">
                        <i class="fas fa-edit"></i> Sửa
                     </a>
                     <a href="@Url.Action("Delete", new { id = sv.MaSV })" class="btn btn-danger btn-sm"
                        onclick="return confirm('Bạn có chắc chắn muốn xóa không?')">
                        <i class="fas fa-trash-alt"></i> Xóa
                     </a>
                  </td>
               </tr>
            }
         </tbody>
      </table>
   </div>
</div>

<!-- Thêm Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
